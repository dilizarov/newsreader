<h2>Feeds</h2>

<ul>
	<% feeds.each ( function (feed) { %>
		<li class="feed">
			<a href="#/feeds/<%= feed.id %>" id="feed<%= feed.id %>">
				<%= feed.escape('title') %>
			</a>
		</li>
	<% }); %>
</ul>